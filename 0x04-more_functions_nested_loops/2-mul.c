#include <stdlib.h>
#include "main.h"
/**
 * mul - multiply two numbers
 * Description:
 * @a: first number
 * @b: second number
 * Return: multiplication result
 */
int mul(int a, int b)
{
int result;
result = a + b;
return (result);
}
