#include <stdio.h>
#include <string.h>
/**
 * _strlen_recursion - returns the length of a string
 * @s: pointer to a string
 * Return: integer
 */
int _strlen_recursion(char *s)
{
return (strlen(s));
}
