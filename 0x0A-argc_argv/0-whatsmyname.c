#include <stdio.h>
/**
 * main - Starting point
 * @argc: arguments counts
 * @argv: arguments vectors
 * Return: 0
 */
int main(int argc __attribute__ ((unused)), char *argv[])
{
printf("%s\n", argv[0]);
return (0);
}
