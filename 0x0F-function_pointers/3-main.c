#include <stdio.h>
#include <string.h>
#include <stdlib.h>
/**
 * main - starting point
 * @argc: arguments counts
 * @argv: arguments vectors
 * Return: 0
 */
int main(int argc, char *argv[])
{
int a, b;
if (argc != 4)
{
printf("Error/n");
}
a = atoi(argv[1]);
b = atoi(argv[3]);
return (0);
}
